name: Deploy to GCP VM
on:
  push:
    branches: [ master ] # Change this if your branch is different

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: SSH and Deploy
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            # This is the path you mentioned
            cd /home/harshilkhatri2808/gitdev
            
            # If your project is inside a subfolder, add 'cd your-repo-name'
            git pull origin master
            
            # Add any build or restart commands here
            # e.g., npm install && pm2 restart all
